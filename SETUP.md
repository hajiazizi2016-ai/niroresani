# راهنمای راه‌اندازی پروژه — برآورد نیرو رسانی

طراحی و توسعه: رسول حاجی عزیزی

---

## این پروژه چیست؟

این، **نسخه اولیه پروژه (فاز ۰ و ۱)** است طبق تحلیل مهندسی تایید‌شده:
- ساختار کامل پروژه چندماژولی (Gradle)
- تم فارسی/راست‌چین کامل (Jetpack Compose)
- ابزار تبدیل تاریخ شمسی و اعداد فارسی
- دیتابیس کامل (Room) با تمام جدول‌های طراحی‌شده در سند
- داده اولیه بانک تجهیزات (طبق ساختار درختی دقیق سند: خط هوایی، پست، مصالح و...)
- صفحه Splash + صفحه اصلی (طبق طراحی UI/UX تایید‌شده)

فازهای بعدی (ثبت شرکت/مشتری، درخت تجهیزات تعاملی، ساخت پیش‌فاکتور، خروجی PDF) در مراحل بعد اضافه می‌شوند.

---

## قدم ۱: باز کردن پروژه

1. فایل ZIP را در یک پوشه دلخواه (مثلاً `C:\Projects\NiroResani`) استخراج کنید.
2. **فیلترشکن را روشن کنید** (Gradle برای دانلود ابزارهای لازم به اینترنت خارجی نیاز دارد).
3. Android Studio را باز کنید.
4. **Open** را بزنید و پوشه استخراج‌شده (`NiroResani`) را انتخاب کنید.
5. صبر کنید تا Gradle Sync تمام شود (نوار پایین صفحه). ممکن است ۵ تا ۱۵ دقیقه طول بکشد (بار اول).

## قدم ۲: افزودن فونت فارسی (اختیاری ولی توصیه‌شده)

در حال حاضر برنامه با فونت پیش‌فرض سیستم اجرا می‌شود تا بدون مشکل باز شود. برای ظاهر حرفه‌ای‌تر:

1. به آدرس زیر بروید و فونت **Vazirmatn** را دانلود کنید:
   `https://github.com/rastikerdar/vazirmatn/releases`
2. از فایل دانلودشده، این دو فایل را بردارید:
   - `Vazirmatn-Regular.ttf` → تغییر نام به `vazirmatn_regular.ttf`
   - `Vazirmatn-Bold.ttf` → تغییر نام به `vazirmatn_bold.ttf`
3. آن‌ها را در مسیر زیر پروژه کپی کنید:
   `app/src/main/res/font/`
4. فایل `app/src/main/java/.../ui/theme/Type.kt` را باز کنید و طبق راهنمای داخل کامنت آن، `AppFontFamily` را به فونت جدید متصل کنید.

## قدم ۳: اجرای برنامه

1. یک گوشی اندرویدی را با کابل USB وصل کنید (حالت "توسعه‌دهنده" و "اشکال‌زدایی USB" را در گوشی فعال کنید) — یا یک شبیه‌ساز (Emulator) در Android Studio بسازید.
2. دکمه سبز رنگ **Run ▶️** بالای Android Studio را بزنید.
3. بعد از چند لحظه، برنامه با یک صفحه شروع آبی رنگ و سپس صفحه اصلی با تاریخ شمسی امروز باز می‌شود.

---

## ساختار پروژه (برای مرجع)

```
NiroResani/
├── app/                          ← ماژول اصلی (UI، Splash، صفحه اصلی)
├── core/
│   ├── common/                   ← ابزار تاریخ شمسی و اعداد فارسی
│   └── database/                 ← دیتابیس Room (تمام جدول‌ها + داده اولیه)
```

## اگر مشکلی پیش آمد

- **خطای Gradle Sync / دانلود ناتمام** → فیلترشکن را چک کنید، دوباره Sync بزنید (آیکون 🔄 بالای صفحه یا File → Sync Project with Gradle Files)
- **خطای "SDK not found"** → مسیر SDK را در File → Settings → Languages & Frameworks → Android SDK بررسی کنید (همانی که قبلاً تنظیم کردید)
- هر خطای دیگری را عیناً برای من کپی کنید تا بررسی کنم.

---

## گام بعدی توسعه

طبق نقشه راه تایید‌شده، فاز بعدی (**فاز ۲: اطلاعات پایه**) شامل ساخت صفحات واقعی ثبت اطلاعات شرکت (با آپلود لوگو) و مدیریت مشتریان است.
