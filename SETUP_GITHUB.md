# راهنمای انتقال پروژه به GitHub — ساخت APK بدون دردسر SDK/Emulator

طراحی و توسعه: رسول حاجی عزیزی

---

## چرا این روش بهتره؟

با این روش، به‌جای اینکه سیستم خودت (با اینترنت محدود و کارت گرافیک قدیمی) مجبور باشه SDK دانلود کنه و Emulator اجرا کنه:

- کد پروژه به گیت‌هاب فرستاده می‌شه
- یک سرور قوی و پرسرعت گوگل/گیت‌هاب (بدون فیلترینگ) خودش برنامه رو می‌سازه
- در چند دقیقه یک فایل **APK آماده نصب** تحویل می‌گیری
- فقط اون فایل رو مستقیم روی گوشیت نصب می‌کنی — بدون ADB، بدون Emulator، بدون SDK Manager

فایل `.github/workflows/build-debug-apk.yml` که از قبل داخل پروژه گذاشته شده، این کار رو خودکار انجام می‌ده.

---

## قدم ۱: ساخت حساب گیت‌هاب (اگه نداری)

1. برو به آدرس: `https://github.com/signup`
2. یک حساب رایگان بساز (ایمیل + رمز عبور)

## قدم ۲: ساخت یک مخزن (Repository) خالی

1. بعد از ورود، بالا-راست صفحه، روی **"+"** بزن → **"New repository"**
2. توی فیلد **"Repository name"** بنویس: `niroresani-app`
3. **مهم:** گزینه‌های "Add README"، "Add .gitignore"، "Choose a license" را **خالی/تیک‌نخورده** بگذار (چون پروژه از قبل کد داره)
4. دکمه سبز **"Create repository"** رو بزن

بعد از ساخته شدن، یک آدرس شبیه این می‌بینی (کپیش کن):
```
https://github.com/USERNAME/niroresani-app.git
```

---

## قدم ۳: آپلود پروژه — دو روش (یکی رو انتخاب کن)

### روش A — ساده‌ترین (بدون نیاز به نصب چیزی، فقط با مرورگر)

1. فایل ZIP پروژه رو که قبلاً دانلود کردی، **دوباره استخراج کن** (یک نسخه تازه، بدون پوشه مخفی `.git`)
2. توی صفحه مخزن تازه‌ساخته‌شده گیت‌هاب، دنبال لینک **"uploading an existing file"** بگرد و بزن
3. تمام فایل‌ها و پوشه‌های داخل پوشه `NiroResani` (نه خود پوشه NiroResani، بلکه محتویاتش: app, core, gradle, build.gradle.kts, settings.gradle.kts, ...) رو با موس بگیر و **بکش و رها کن (Drag & Drop)** داخل کادر آپلود گیت‌هاب
4. پایین صفحه یک پیام commit بنویس (مثلاً "آپلود اولیه پروژه") و دکمه **"Commit changes"** رو بزن

### روش B — با نرم‌افزار Git (اگه راحت‌تری)

1. اگه Git نصب نیست، از `https://git-scm.com/download/win` دانلود و نصب کن
2. توی پوشه پروژه (همون `NiroResani`)، راست‌کلیک کن → **"Open Git Bash here"** یا از همون ترمینال Android Studio استفاده کن
3. این دستورات رو یکی‌یکی بزن (به‌جای USERNAME اسم کاربری گیت‌هابت رو بذار):

```
git remote add origin https://github.com/USERNAME/niroresani-app.git
git branch -M main
git push -u origin main
```

4. اگه رمز خواست، به‌جای رمز عادی گیت‌هاب، باید یک **Personal Access Token** بسازی (گیت‌هاب دیگه رمز ساده قبول نمی‌کنه) — از مسیر:
   `GitHub → Settings → Developer settings → Personal access tokens → Generate new token`

---

## قدم ۴: دیدن ساخت خودکار APK

1. بعد از آپلود، توی صفحه مخزن گیت‌هابت، برو به تب **"Actions"** (بالای صفحه)
2. باید یک اجرا (Run) با نام **"Build Debug APK"** ببینی که یا در حال اجراست (نقطه زرد چرخان) یا تموم شده (تیک سبز ✅)
3. صبر کن ۳ تا ۷ دقیقه تا تیک سبز بشه

## قدم ۵: دانلود APK و نصب روی گوشی

1. روی همون Run با تیک سبز کلیک کن
2. پایین صفحه، بخش **"Artifacts"** رو پیدا کن
3. روی **"NiroResani-debug-apk"** کلیک کن تا دانلود بشه (یک فایل ZIP کوچیک که فایل APK توشه)
4. فایل ZIP رو استخراج کن، فایل `app-debug.apk` رو از طریق تلگرام/بلوتوث/کابل به گوشیت بفرست
5. روی فایل APK توی گوشی بزن → **Install**
   (ممکنه گوشی هشدار بده "منبع ناشناس" — اجازه نصب رو بده، چون فایل خودمونه)

---

## بعد از این

هر بار که کد پروژه تغییر کنه (فازهای بعدی توسعه)، کافیه فایل‌های جدید رو دوباره توی گیت‌هاب آپلود کنی (یا `git push` بزنی) — به‌صورت خودکار یک APK جدید ساخته می‌شه، دیگه نیازی به این مراحل طولانی SDK/Emulator نیست.
